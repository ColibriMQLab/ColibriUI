import React from 'react';

import Separator from '.';

export default { 
    title: 'UI/Separator',
    parameters: {
        layout: "centered",
    },
};

export const Default: React.FC = (): JSX.Element => <Separator />;
